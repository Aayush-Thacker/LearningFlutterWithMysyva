class ProfileDetails {
  String asset, name, details;

  ProfileDetails(this.asset, this.name, this.details);

  String getAsset() => this.asset;
  String getName() => this.name;
  String getDetails() => this.details;
}
